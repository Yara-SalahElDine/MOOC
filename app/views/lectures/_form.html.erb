<%= form_with(model: lecture, local: true) do |form| %>
  <% if lecture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lecture.errors.count, "error") %> prohibited this lecture from being saved:</h2>

      <ul>
      <% lecture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.cktext_area :content, id: :lecture_content %>
  </div>

  <div class="field">
    <%= form.label :attachement %>
    <%= form.file_field :attachement, id: :lecture_attachement %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.select :course_id, Course.all.collect{|course| [course.title, course.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
